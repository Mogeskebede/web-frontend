apiVersion: v1
kind: Service
metadata:
  name: web-frontend
  namespace: orders-platform
spec:
  selector:
    app: web-frontend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP



